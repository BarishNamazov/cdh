# Implementing Concepts

Concept implementations are independent classes under `src/concepts/<Name>/`. Action methods accept one object parameter and return an object. Query methods start with `_` and return arrays.
