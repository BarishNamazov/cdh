# Testing Concepts

Concept tests instantiate concepts with `track(...)`, use `testAction(...)` for action cases, use `expectError(...)` for requires failures, and narrate principle or multi-action tests with `trace(...)`.
