# Violation Fixtures

Each directory is a minimal target repo for one CDH rule. These fixtures are intentionally small and are not required to pass unrelated checks until WP1 wires precise rule-scoped tests.
