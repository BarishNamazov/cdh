{
  "name": "{{name}}",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@mit-sdg/sync-engine": "*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
